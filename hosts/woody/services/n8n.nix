{
  ...
}:

{
  # Temporarily disabled due to hash mismatch in nixpkgs
  # Re-enable after nixpkgs updates or overlay fix is applied
  # services.n8n =  {
  #   enable = true;
  #   openFirewall = true;
  # };
}
