# -----------------------------------------------------
# Dialog and Authentication Windows
# -----------------------------------------------------
# Rules for system dialogs and authentication prompts

windowrule = match:class ^(gcr-prompter)$, dim_around on
windowrule = match:class ^(xdg-desktop-portal-gtk)$, dim_around on
windowrule = match:class ^(polkit-gnome-authentication-agent-1)$, dim_around on
