# -----------------------------------------------------
# Screenshot Tools
# -----------------------------------------------------
# Rules for screenshot applications (should be invisible/fast)

# Window rules for screenshot tools
windowrule = match:class ^(grim)$, no_anim on
windowrule = match:class ^(slurp)$, no_anim on
windowrule = match:class ^(grimblast)$, no_anim on

# Layer rules for hyprshot (remove 1px border around screenshots)
# layerrule = noanim, selection  # Removed: invalid layer rule

