# vim:set ft=tmux:
%hidden MODULE_NAME="iplocal"

set -ogq "@forceline_${MODULE_NAME}_icon" "ip "
set -ogqF "@forceline_${MODULE_NAME}_color" "#{E:@thm_blue}"
set -ogq "@forceline_${MODULE_NAME}_text" "#{l:#{iplocal}}"

source -F "#{d:current_file}/../utils/status_module.conf"
