# vim:set ft=tmux:
%hidden MODULE_NAME="directory"

set -ogq "@forceline_${MODULE_NAME}_icon" "  "
set -ogqF "@forceline_${MODULE_NAME}_color" "#{@fl_base15}"  # bright cyan/sky
set -ogq "@forceline_${MODULE_NAME}_text" " #{b:pane_current_path}"

source -F "#{d:current_file}/../utils/status_module.conf"
