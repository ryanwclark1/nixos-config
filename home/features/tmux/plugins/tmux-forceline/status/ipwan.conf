# vim:set ft=tmux:
%hidden MODULE_NAME="ipwan"

set -ogq "@forceline_${MODULE_NAME}_icon" "wanip "
set -ogqF "@forceline_${MODULE_NAME}_color" "#{E:@thm_blue}"
set -ogq "@forceline_${MODULE_NAME}_text" ""

source -F "#{d:current_file}/../utils/status_module.conf"
