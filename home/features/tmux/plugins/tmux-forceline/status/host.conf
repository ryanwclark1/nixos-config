# vim:set ft=tmux:
%hidden MODULE_NAME="host"

set -ogq "@forceline_${MODULE_NAME}_icon" "󰒋 "
set -ogqF "@forceline_${MODULE_NAME}_color" "#{@fl_secondary}"  # purple/mauve
set -ogq "@forceline_${MODULE_NAME}_text" " #H"

source -F "#{d:current_file}/../utils/status_module.conf"
