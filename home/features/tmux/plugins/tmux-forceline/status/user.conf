# vim:set ft=tmux:
%hidden MODULE_NAME="user"

set -ogq @forceline_user_icon " "
set -ogqF @forceline_user_color "#{@thm_sky}"
set -ogq @forceline_user_text "#(whoami)"

source -F "#{d:current_file}/../utils/status_module.conf"
