# vim:set ft=tmux:
%hidden MODULE_NAME="time"

set -ogq "@forceline_${MODULE_NAME}_icon" " "
set -ogqF "@forceline_${MODULE_NAME}_color" "#{E:#{@fl_primary}}"
set -ogq "@forceline_${MODULE_NAME}_text" "%H:%M"

source -F "#{d:current_file}/../utils/status_module.conf"
