# vim:set ft=tmux:
%hidden MODULE_NAME="battery"

set -ogq "@forceline_${MODULE_NAME}_icon" "󰁹 "
set -ogqF "@forceline_${MODULE_NAME}_color" "#{@fl_success}"
set -ogq "@forceline_${MODULE_NAME}_text" "#{l:#{battery_percentage}}"

source -F "#{d:current_file}/../utils/status_module.conf"
