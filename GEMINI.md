# Gemini CLI Mandates

- **Source Control Safety:** NEVER use destructive git commands (e.g., `git restore`, `git reset`, `git checkout .`) on unstaged changes. Always check with the user before discarding any work.
- **Context Efficiency:** Follow the guidelines in the system prompt for minimizing context usage.
